关于有效括号的题，如678，用计算左右括号值的方法无法实现，为何总是想到用这个方法？可能是因为某个题是总是合法的或者自己构建（dfs？）左右括号的时候
才能用这个判断。栈有时还得装下标（678），不是一定是装括号或者字符的
basicCalculator---》用cur，不用预先push 空string
basic calculator2---》只用一个stack，不用signal，遇到符号直接把后面得数字搞出来然后直接算，如遇到加减就是
                         //stack里放正负数，遇到乘除就直接算出来再push进去，最后再遍历stack 把所有元素加起来就是答案

856----》decode string，要不要写cur都行，写的是要预先push空string

678----》原来star也要stack来装，因为也是有位置的约束，因为星号不能跨区域用